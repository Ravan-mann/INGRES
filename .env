GOOGLE_API_KEY=AIzaSyDuSJ45-gcJxvcmNREoAMt1y4GBw0KQ_Hc
import os
from pymongo import MongoClient
from pymongo.errors import ConnectionFailure
from dotenv import load_dotenv  # <-- Add this line

load_dotenv()  # <-- And this line to load variables from .env

# The rest of your code remains the same.
# MONGO_URI will now be loaded from your .env file.
MONGO_URI = os.environ.get("MONGO_URI")

if not MONGO_URI:
    print("Error: MONGO_URI environment variable not found.")
    print("Please create a .env file or set the environment variable manually.")
    exit()

client = None
try:
    client = MongoClient(MONGO_URI)
    client.admin.command('ping')
    print("✅ MongoDB connection successful.")
    
    db = client.get_default_database()
    print(f"Default database: '{db.name}'")

except ConnectionFailure as e:
    print(f"Could not connect to MongoDB: {e}")
except Exception as e:
    print(f"An error occurred: {e}")

finally:
    if client:
        client.close()
        print("MongoDB connection closed.")
import os
from pymongo import MongoClient
from pymongo.errors import ConnectionFailure
from dotenv import load_dotenv  # <-- Add this line

load_dotenv()  # <-- And this line to load variables from .env

# The rest of your code remains the same.
# MONGO_URI will now be loaded from your .env file.
MONGO_URI = os.environ.get("MONGO_URI")

if not MONGO_URI:
    print("Error: MONGO_URI environment variable not found.")
    print("Please create a .env file or set the environment variable manually.")
    exit()

client = None
try:
    client = MongoClient(MONGO_URI)
    client.admin.command('ping')
    print("✅ MongoDB connection successful.")
    
    db = client.get_default_database()
    print(f"Default database: '{db.name}'")

except ConnectionFailure as e:
    print(f"Could not connect to MongoDB: {e}")
except Exception as e:
    print(f"An error occurred: {e}")

finally:
    if client:
        client.close()
        print("MongoDB connection closed.")
import os
from pymongo import MongoClient
from pymongo.errors import ConnectionFailure
from dotenv import load_dotenv  # <-- Add this line

load_dotenv()  # <-- And this line to load variables from .env

# The rest of your code remains the same.
# MONGO_URI will now be loaded from your .env file.
MONGO_URI = os.environ.get("MONGO_URI")

if not MONGO_URI:
    print("Error: MONGO_URI environment variable not found.")
    print("Please create a .env file or set the environment variable manually.")
    exit()

client = None
try:
    client = MongoClient(MONGO_URI)
    client.admin.command('ping')
    print("✅ MongoDB connection successful.")
    
    db = client.get_default_database()
    print(f"Default database: '{db.name}'")

except ConnectionFailure as e:
    print(f"Could not connect to MongoDB: {e}")
except Exception as e:
    print(f"An error occurred: {e}")

finally:
    if client:
        client.close()
        print("MongoDB connection closed.")
import os
from pymongo import MongoClient
from pymongo.errors import ConnectionFailure
from dotenv import load_dotenv  # <-- Add this line

load_dotenv()  # <-- And this line to load variables from .env

# The rest of your code remains the same.
# MONGO_URI will now be loaded from your .env file.
MONGO_URI = os.environ.get("MONGO_URI")

if not MONGO_URI:
    print("Error: MONGO_URI environment variable not found.")
    print("Please create a .env file or set the environment variable manually.")
    exit()

client = None
try:
    client = MongoClient(MONGO_URI)
    client.admin.command('ping')
    print("✅ MongoDB connection successful.")
    
    db = client.get_default_database()
    print(f"Default database: '{db.name}'")

except ConnectionFailure as e:
    print(f"Could not connect to MongoDB: {e}")
except Exception as e:
    print(f"An error occurred: {e}")

finally:
    if client:
        client.close()
        print("MongoDB connection closed.")
import os
from pymongo import MongoClient
from pymongo.errors import ConnectionFailure
from dotenv import load_dotenv  # <-- Add this line

load_dotenv()  # <-- And this line to load variables from .env

# The rest of your code remains the same.
# MONGO_URI will now be loaded from your .env file.
MONGO_URI = os.environ.get("MONGO_URI")

if not MONGO_URI:
    print("Error: MONGO_URI environment variable not found.")
    print("Please create a .env file or set the environment variable manually.")
    exit()

client = None
try:
    client = MongoClient(MONGO_URI)
    client.admin.command('ping')
    print("✅ MongoDB connection successful.")
    
    db = client.get_default_database()
    print(f"Default database: '{db.name}'")

except ConnectionFailure as e:
    print(f"Could not connect to MongoDB: {e}")
except Exception as e:
    print(f"An error occurred: {e}")

finally:
    if client:
        client.close()
        print("MongoDB connection closed.")
import os
from pymongo import MongoClient
from pymongo.errors import ConnectionFailure
from dotenv import load_dotenv  # <-- Add this line

load_dotenv()  # <-- And this line to load variables from .env

# The rest of your code remains the same.
# MONGO_URI will now be loaded from your .env file.
MONGO_URI = os.environ.get("MONGO_URI")

if not MONGO_URI:
    print("Error: MONGO_URI environment variable not found.")
    print("Please create a .env file or set the environment variable manually.")
    exit()

client = None
try:
    client = MongoClient(MONGO_URI)
    client.admin.command('ping')
    print("✅ MongoDB connection successful.")
    
    db = client.get_default_database()
    print(f"Default database: '{db.name}'")

except ConnectionFailure as e:
    print(f"Could not connect to MongoDB: {e}")
except Exception as e:
    print(f"An error occurred: {e}")

finally:
    if client:
        client.close()
        print("MongoDB connection closed.")
import os
from pymongo import MongoClient
from pymongo.errors import ConnectionFailure
from dotenv import load_dotenv  # <-- Add this line

load_dotenv()  # <-- And this line to load variables from .env

# The rest of your code remains the same.
# MONGO_URI will now be loaded from your .env file.
MONGO_URI = os.environ.get("MONGO_URI")

if not MONGO_URI:
    print("Error: MONGO_URI environment variable not found.")
    print("Please create a .env file or set the environment variable manually.")
    exit()

client = None
try:
    client = MongoClient(MONGO_URI)
    client.admin.command('ping')
    print("✅ MongoDB connection successful.")
    
    db = client.get_default_database()
    print(f"Default database: '{db.name}'")

except ConnectionFailure as e:
    print(f"Could not connect to MongoDB: {e}")
except Exception as e:
    print(f"An error occurred: {e}")

finally:
    if client:
        client.close()
        print("MongoDB connection closed.")
